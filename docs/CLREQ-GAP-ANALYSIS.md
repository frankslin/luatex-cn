# CLReq 符合度差距分析与实施规划

> 对象：`luatex-cn` v0.3.8
> 依据：W3C《Requirements for Chinese Text Layout / 中文排版需求》（clreq）Editor's Draft，
> 抓取自 `w3c/clreq` 仓库 `gh-pages` 分支的 `index.html`（2026-07 抓取；W3C TR 快照与 ED 在本文
> 涉及的条款上无实质差异）。
> 本文只做分析与规划，不含代码改动。

---

## 0. 摘要

- **clreq 与本项目的定位存在部分错位**：clreq 明确写道「至于古籍排版，非本文件所及」，
  而本项目的主战场（`ltc-guji`、句读、夹注、眉批、版心鱼尾）恰恰是古籍。因此
  「完整 CLReq 实现」的收益主要落在 **`ltc-cn-vbook` / `ltc-tw-vbook` 两个现代竖排文档类**上，
  古籍类只需保证不冲突。
- **当前覆盖率（估算）**：以「只做直排」为口径，clreq 条款覆盖约 **45%–50%**；
  若把横排条款也计入分母，约 **25%–30%**。页面版式（版心/行长/行数/行距/分栏）覆盖最好（约 70%），
  行内调整（挤压/拉伸优先顺序）覆盖最差（约 20%，基本缺位）。
- **最大的单点缺口不是功能，而是模型**：clreq 的行内调整要求「每个间隙带有可挤压/可拉伸量与优先级」，
  而当前 `layout-grid` 的 grid 模式是**刚性等距格**，natural 模式是**整列均分间隙**，两者都没有
  「可调空间链 + 优先级求解」这一层。标点挤压、禁则的「先挤进后推出」、中西间距、均排、
  纵横对齐全部依赖它。**这一层是整个工程的地基，必须先建。**
- **工作量估算**：直排口径下达到「可宣称的 clreq 子集符合」约 **100–180 人日**（≈5–9 人月）；
  若连横排引擎一并做（真正意义上的「完整」）约 **145–260 人日**（≈7–13 人月）。
- **建议口径**：不追求 100%，而是发布一份 **《clreq 符合度声明矩阵》**，逐条标注
  「实现 / 部分实现 / 不适用（横排）/ 不实现（并说明理由）」，并把 P0–P2 作为第一个里程碑。

---

## 1. 依据与方法

1. 抓取 clreq 全文，按 `lang="zh-hans"` 过滤出简体中文正文，得到约 4.6 万字的条款文本。
2. 按 clreq 的章节结构（文本方向 / 字形 / 排版单元 / 标点与行内 / 行与段落 / 页面与书籍 / 标点表）
   逐节抽取「可实现的排版要求」，剔除纯背景叙述。
3. 对每条要求在本仓库中定位实现证据（文件:行），标注状态：
   ✅ 已实现　🟡 部分实现　❌ 未实现　➖ 不适用（横排专属或非排版引擎职责）。

---

## 2. 范围界定：三种「完整实现」口径

| 口径 | 含义 | 是否推荐 |
|------|------|----------|
| **A. 直排子集** | 实现 clreq 中所有与直排相关的条款；横排条款标注「不适用，交由 ctex/xeCJK 生态」 | 可行 |
| **B. 直排 + 横排注音** | A 之外，为汉语拼音标音单独实现一个横排的注音行（clreq 的罗马拼音标音仅横排） | 视需求 |
| **C. 全覆盖** | 横排与直排两条后端，共享同一套 clreq 规则内核 | ✅ **推荐** |

> **口径已修订（见 `docs/CLREQ-HORIZONTAL-PLAN.md`）**：本文初版把横排排在最后并标注「不建议」，
> 理由是与 `ctex`/`xeCJK` 重复。该判断漏掉了两点——① 横排可直接用 TeX 的断行器作为求解器，
> 同一条 clreq 条款的实现成本约为竖排的 1/3；② 真正需要共享的是「带优先级的一维空间分配器」这个
> 纯函数，横排是验证它的最低成本场地。因此**改为横排先行、共享内核回流竖排**。
> 后文第 6 节的 P0–P5 仍然有效，但 P0 已被横排规划中的 H0（共享内核）取代并扩大，
> P1/P2/P4 的工作量因复用而下调（见横排规划第 5 节）。

下文的 P0–P5 描述的是**竖排后端**要补的条款，仍按原顺序保留。

---

## 3. 逐章差距总表

| clreq 章节 | 关键要求 | 现状 | 覆盖 |
|-----------|---------|------|------|
| 2 文本方向 | 直排文本流、右侧装订、左页起 | 竖排网格引擎完备；`twoside` 镜像边距已有 | 🟡 70% |
| 2 文本方向 | 直排中西文三种配置（全角直立 / 旋转 90° / 中横排） | 仅逐字入格（近似"直立"）；旋转仅对 `—`/`…` 生效 | ❌ 20% |
| 2 文本方向 | 双向文本（bidi） | 无 | ❌ 0% |
| 3 字形 | 宋/楷/黑/仿宋四体、字重 | 字体族探测与 fallback 完善，但无语义化字体角色 | 🟡 60% |
| 3 字形 | 基于上下文的字形变形（`vert`/`vrt2`） | punct 模块自建映射表 + PUA/tounicode 兜底 | 🟡 65% |
| 4 排版单元 | 中西混排字距（1/4 em） | 无任何自动中西间距 | ❌ 0% |
| 4 排版单元 | 儿化小字（U+16FF2/3） | 无 | ❌ 0% |
| 5 标点 | 点号/标号分类、大陆式偏靠 vs 港台式居中 | 6 类分类 + `mainland`/`taiwan` 双风格 | 🟡 60% |
| 5 标点 | 直排引号替换（弯引号→直角引号） | 已实现 | ✅ 95% |
| 5 标点 | 连接号、间隔号、分隔号、破折号/省略号（占两字、不可拆） | 未分类、未做两字宽单元 | ❌ 10% |
| 5 标点 | 非典型标点（科技文献句点、叹问号叠加、示亡号） | 无（示亡号可用 `\decorate` 手工近似） | ❌ 5% |
| 5 强调 | 着重号 | `\着重号` 已有，但默认为古籍圈点 `○` 而非规范的 `●` | 🟡 60% |
| 5 行间标点 | 专名号、书名号甲式（波浪线）、双面装 | `\专名号`/`\书名号` 已有，线在左、着重在右，分段正确 | 🟡 75% |
| 5 行间标点 | 相邻标注两侧缩短（≤1/8 em）、行距下限校验 | 无 | ❌ 0% |
| 5 行内注 | 行间批语（直排在基文左侧） | 侧批/夹注/眉批体系成熟，等价能力强 | ✅ 85% |
| 5 行内注 | 注音符号标音（右侧、3:10 比例、声调位置、分离禁则） | 完全没有 | ❌ 0% |
| 5 行内注 | 罗马拼音标音、中外文对照、词对齐 | 完全没有 | ❌ 0% |
| 6 换行 | 行首行尾禁则（四级：不处理/基本/GB/严格） | 实现了「基本处理」的一部分，级别不可配置 | 🟡 45% |
| 6 换行 | 符号分离禁则（两字标点、数字、%/°、±、货币、注释符号） | 无 | ❌ 0% |
| 6 换行 | 行尾点号悬挂 | 代码中明确标注「未实现，留待将来」 | ❌ 0% |
| 6 对齐 | 段首缩排、段落缩排、凸排 | 缩进体系完善（含抬头/挪抬），凸排需手工 | 🟡 75% |
| 6 对齐 | 单行对齐四种（居中/行首/行尾/均排） | `vertical-align` 提供前三种，**均排缺失** | 🟡 60% |
| 6 行内调整 | 挤压优先顺序（7 级）、拉伸优先顺序（2 级 + 兜底） | 无优先级模型；natural 模式仅做整列均分 | ❌ 20% |
| 6 行内调整 | 纵横对齐下的中西混排（西文占汉字整数倍） | 无 | ❌ 0% |
| 6 密排/疏排 | 密排、疏排、紧排、均排 | 密排 ✅、疏排可用 `cell_gap` 近似、均排 ❌ | 🟡 55% |
| 7 页面 | 版心（type area）设计：字号/行长/行数/行距/栏数/栏距 | 完整，且有 band 分栏 | ✅ 90% |
| 7 页面 | 页眉、页码位置 | 版心与 vbook header 双套方案 | ✅ 85% |
| 7 页面 | 孤行孤字（"孤字不成行、孤行不成页"） | 无任何处理 | ❌ 0% |
| 7 标题 | 标题占行、单页起/换页、同行标题、跨栏标题 | `\换页` 有；占行/单页起/同行标题缺 | 🟡 30% |
| 8 表单交互 | — | ➖ 不适用 | ➖ |
| 附录 标点表 | 全表 + 「不可分离」「直排右旋 90°」两列 | 分散在若干 Lua 表中，未数据化 | 🟡 40% |

---

## 4. 详细差距分析

### 4.1 标点符号系统（`tex/core/luatex-cn-core-punct.lua`）

**已有能力**

- 6 类分类 `CL_OPEN / CL_CLOSE / CL_FULLSTOP / CL_COMMA / CL_MIDDLE / CL_NOBREAK`
  （`luatex-cn-core-punct.lua:137-206`）。
- 直排字形替换表 `VERT_FORM_MAP`，含括号类、引号类、`—`、`…`（同上 `:212-242`）；
  字体缺字时对 `—`/`…` 退化为旋转（`:572-576`）。
- 字体缺乏标准码位（`vert` GSUB 落到 PUA）时，通过 `tounicode` 反查还原（`:264-302`）。
- 标点字面墨心测量与补偿（`:65-129`），这是实现「字面始端 / 字面居中」两种字面分布的良好基础。
- 大陆式偏靠偏移 `MAINLAND_OFFSETS`（`:766-772`）。

**关键差距**

1. **挤压是无条件的**。`DEFAULT_SQUEEZE = 0.5`（`:634-641`）与 natural 模式的
   `get_cell_height`（`layout-grid-helpers.lua:270-298`）都会把逗号/顿号/开闭括号
   **一律压成半格**，不区分上下文。clreq 规定挤压只在两种情形发生：
   **① 连续标点相邻；② 标点位于行首/行尾**（`punctuation_width_adjustment` 节）。
   现行做法在「一个逗号夹在汉字之间」时也压缩，属于风格越界。
2. **缺少「相邻两标点占 1.5 字宽」规则**。clreq 的原则调整量是 2 → 1.5 字宽，
   风格上才允许进一步到 1 字宽；且挤压方向须让夹注符号紧贴被夹注内容。
3. **缺少大量标点类别**：连接号（`–`/`～`）、间隔号（`·`，港台占一字、大陆占半字）、
   分隔号（`/`）、破折号与省略号的**两字宽整体单元**、叹问号叠加（`?!`/`!?`/`???`）。
4. **`CL_NOBREAK` 名不副实**：定义了却没有任何「不可拆成两行」的执行逻辑。
5. **偏移量是经验值**（`x=0.20, y=0.25`），而非按 clreq 的「字面始端」定义 + 字体度量推导。
   墨心测量基础设施已有，应改为度量驱动。

### 4.2 换行禁则与行内调整（最大缺口）

**已有能力**

- grid 模式的 kinsoku 钩子：向前看一个字符，若下一字符禁止行首则把当前字符一起推到下一列；
  若当前字符禁止行尾也推出（`luatex-cn-core-punct.lua:399-486`）。
- natural 模式的 kinsoku：比较「挤进」与「推出」两种方案的每间隙代价，取代价小者
  （`layout-grid.lua:1552-1626`）。这是最接近 clreq「先挤进，后推出」的一处实现。

**关键差距**

1. **禁则级别不可配置**。clreq 定义四级（不处理 / 基本处理 / GB 法 / 严格处理），
   现在硬编码为「基本处理」的子集：行首禁止 `close/fullstop/comma/middle`、
   行尾禁止 `open`（`punct.lua:340-352`、`layout-grid.lua:1544-1550`）。
   缺连接号/间隔号/分隔号的行首禁止、缺分隔号的行尾禁止、缺严格级的破折号/省略号行首禁止。
2. **符号分离禁则整体缺失**：两字宽标点不得拆列、阿拉伯数字整体不拆、
   `数字+%/‰/°/℃`、`+/-/± + 数字`、货币符号 + 数字、上下标与注释记号不得与被标记文字分离。
3. **行尾点号悬挂未实现**，代码里有明确的 TODO（`punct.lua:669-671`）。
   clreq 指出悬挂**适用于直排**（横排港台式因点号居中不宜悬挂），正是本项目的主场。
4. **没有挤压/拉伸优先级引擎**。clreq 的挤压顺序为：
   行末标点 → 西文词距 → 间隔号 → 夹注符号 → 逗号/顿号/分号 → 中西间距 → 句号/问号/叹号；
   拉伸顺序为：西文词距 → 中西间距 → 兜底均分（且禁止拉伸不可分离单元、避免拉伸连接号/分隔号两侧）。
   grid 模式完全没有这一层；natural 模式只有「整列 gap 均分」。
5. **均排（justify）缺失**：`vertical-align` 只有 top（默认）/center/bottom
   （键定义在 `luatex-cn-core-content.sty:129-130`，取值校验在
   `luatex-cn-layout-grid.lua:542`），没有把余量平均分配到字间的模式。

### 4.3 中西文混排

clreq 要求直排下西文有三种配置：等宽/全角直立、顺时针旋转 90°、以及缩略词整体入格；
横排下汉字与西文之间加不超过 1/4 汉字宽的间距，且中西间距参与行内调整（可挤到 1/8、拉到 1/2）；
纵横对齐时西文所占空间应为汉字的整数倍。

现状：引擎按字形逐个放入格子，`cell_height` 取字号（`layout-grid-helpers.lua:270-298`），
比例西文字母会各占一格且横向不居中；`ATTR_VERT_ROTATE` 只在标点缺字时使用
（`render-page-process.lua:137-140` 有完整的旋转 + 平移实现，**可复用**）；
没有任何中西间距插入逻辑。`\填充文本框` 可以手工模拟「中横排」，但不是自动机制。

### 4.4 行间注与注音

- **行间批语**：本项目的 `\侧批`/`\夹注`/`\眉批` 体系成熟，覆盖 clreq「行间批语位基文左侧、
  长度不拘、可换色」的要求，甚至超出。
- **注音符号（ㄅㄆㄇ）标音**：完全缺失。clreq 给了非常具体的度量：注文占基字**一半**空间、
  单符号与基字比 **3:10**、平上去调号在末符号右上方（直式向上回返半个符号空间）、
  入声调号在整体外侧右下、轻声在字音前方且高度与基字比 **1:15**、
  直排时注文整体垂直居中于基字、注音不得单独出现在行首。
  这一块**规范最细、最可测试**，对 `ltc-tw-vbook` 价值最高。
- **罗马拼音标音 / 中外文对照 / 词对齐**：仅横排，属口径 B。

### 4.5 页面与书籍版式

- 版心（type area）设计要素（字号、行长、行数、行距、栏数、栏距、版心相对成品尺寸的位置）
  基本齐备，band 分栏可对应 clreq 的「分栏」。**术语提醒**：clreq 的「版心」= type area，
  与本项目中「版心」= 书页中缝（鱼尾/象鼻）不是一回事，文档中需要显式消歧。
- **孤行孤字完全没有处理**（全仓库无 widow/orphan 相关代码）。clreq 给出的处理手段
  （从前行借一字并对该行均排）依赖 4.2 的均排能力。
- 标题处理：缺「占行」（标题占基本版式的整数倍行数）、「单页起」（奇偶页起）、
  「同行标题」（标题与正文同列、间隔一个全角空白）、「跨栏标题」。
- 号数 ↔ 点数换算表（初号 42pt … 七号 5.25pt）可以直接做成字号别名，成本极低、体验收益明显。

---

## 5. 架构前提与风险

| # | 风险 | 说明 | 应对 |
|---|------|------|------|
| R1 | **刚性网格 vs 弹性调整** | clreq 的行内调整本质是「带优先级的可调空间链」，与固定格模型冲突 | P2 引入 `cell = {size, shrink, stretch, priority}` 的统一模型，grid 模式退化为 shrink=stretch=0 的特例 |
| R2 | **grid / natural 双轨制** | 同一规则要写两遍（现在 kinsoku、标点挤压都已经各写了一份），长期必然发散 | P2 统一到一套求解器，两种模式只是参数差异 |
| R3 | **像素回归测不出规范符合性** | 若 bug 已进基线，`regression_test.py` 无法发现 | 扩展 `test/geometry_test.py` 思路，建 **clreq 断言测试**：解析 PDF 内容流，直接断言「逗号占半格」「注音符号与基字比 3:10」等度量 |
| R4 | **字体依赖** | 注音符号、`vert` 特性、波浪线字形都依赖字体支持 | 在 `scripts/download_fonts.py` 的 manifest 中固定一套含注音符号的测试字体；缺字时给出明确 warning 而非静默降级 |
| R5 | **古籍类回归** | 标点挤压规则改为「上下文相关」会改变现有古籍示例的版面 | 规则按文档类分档：`ltc-guji` 保持现状为默认，新规则在 vbook 类启用；每一步都跑 basic/past_issue/complete 三套基线 |
| R6 | **规范本身的可选性** | clreq 大量条款是「排版风格」而非强制（如是否挤压、禁则级别） | 所有新规则都做成可配置键，并提供 `大陆`/`台湾`/`不调整` 三套预设 |

---

## 6. 路线图

每个阶段都可独立发布，遵循项目「小步提交 + 先 unit test 再 regression test」的既有纪律。

### P0 · 基础设施与数据化（前置，必须先做）

- 把 clreq 附录《标点符号全表》数据化为单一数据源
  `tex/core/luatex-cn-punct-table.lua`：字符 → {类别, 字面分布(大陆/港台), 宽度, 可调空间(始/末/两侧),
  是否不可分离, 直排是否右旋, 是否行首禁则, 是否行尾禁则}。现有分散的 `CL_*`/`VERT_FORM_MAP` 改为从它派生。
- 新建 `test/clreq_test.py`：解析 PDF 内容流，提供「第 N 列第 M 字的格心坐标 / 实际占位」断言原语，
  并建立 `test/clreq_test/` 用例目录（每条 clreq 条款一个最小用例）。
- 新建 `docs/CLREQ-CONFORMANCE.md` 符合度矩阵骨架（逐条：条款 → 状态 → 用例 → 备注），
  后续每个阶段只更新状态列。

**验收**：标点表单测通过；符合度矩阵可生成；至少 10 条断言用例跑通。
**工作量：8–12 人日**

### P1 · 标点系统达标

- 扩类：连接号、间隔号、分隔号、破折号/省略号（两字宽单元）、叹问号叠加。
- 宽度调整改为上下文相关：仅在「相邻标点」与「行首/行尾」触发；实现 2→1.5 字宽原则调整，
  并提供 `紧凑`（→1 字宽）风格开关；夹注符号挤压方向紧贴被夹注内容。
- 字面分布从经验偏移改为度量驱动（复用已有 ink-center 测量）。
- 港台居中式与大陆偏靠式各自的「不可调整」集合（如直排的 `：；？！` 固定一字宽）。
- `\着重号` 在 vbook 类默认改为实心圆点 `●/•`，并实现「标点上不加着重号」。

**验收**：clreq 5.x 相关断言用例全绿；三套回归基线在 `ltc-guji` 下**零变化**。
**工作量：18–28 人日**

### P2 · 弹性行内调整引擎（核心，风险最高）

- 引入统一 cell 模型 `{size, shrink, stretch, priority}`，`flush_buffer` 前先跑「行内调整求解器」。
- 实现 clreq 挤压 7 级优先顺序与拉伸 2 级 + 兜底均分，含各项的上下限（西文词距最小 1/4、
  中西间距 1/8–1/2、标点最小半字等）。
- 禁则重做：四级可配置 + 「先挤进，后推出」两阶段；符号分离禁则。
- 行尾点号悬挂（直排默认可开）。
- 均排（justify）作为单行对齐的第四种模式。
- grid / natural 两模式统一到同一求解器。

**验收**：行内调整断言用例全绿；`ltc-cn-vbook`/`ltc-tw-vbook` 基线更新并人工评审；
`ltc-guji` 默认行为不变。
**工作量：25–45 人日**

### P3 · 中西文混排

- 直排三种西文配置：全角直立 / 顺时针旋转 90°（复用 `render-page-process.lua` 的旋转实现）/
  短串整体入格（自动「中横排」），并提供 `\直立{}`、`\横置{}`、`\中横排{}` 显式命令。
- 中西间距自动插入（默认 1/4 字宽），并接入 P2 的可调空间链。
- 纵横对齐模式：西文串占用汉字整数倍宽度。
- 西文单词与数字串不可断行；数字串整体不拆。

**验收**：混排断言用例全绿；新增混排示例文档。
**工作量：14–24 人日**

### P4 · 行间注 · 注音符号

- 注音符号标音（直排右侧）：注文占基字一半空间、符号与基字 3:10、
  平上去/入声/轻声三类调号定位、注文垂直居中于基字、注音分离禁则。
- 与已有 linemark/decorate 的侧位冲突仲裁（右侧同时有着重号与注音时的让位规则）。
- 中外文对照的词对齐（注文短于基文→加大注文字距；长于基文→加大基文字距）。
- 罗马拼音标音（横排）**列入口径 B**，本阶段只做数据结构预留。

**验收**：注音度量断言用例全绿；`ltc-tw-vbook` 增加注音示例。
**工作量：18–35 人日**

### P5 · 页面与标题级规则

- 孤行孤字：段末孤字（从前行借一字 + 该行均排）、页首孤行（挤入前页/前页借一行），可配置严格度。
- 标题「占行」（占基本版式整数倍行数）、单页起（奇偶页起）、同行标题（一个全角空白间隔）、跨栏标题。
- 号数字号别名（初号…七号）。
- 换页/换栏时前一页留空规则（直排多栏行数可不齐）。

**验收**：页面级断言用例全绿；示例书籍端到端复核。
**工作量：12–20 人日**

### P6 · 横排模式 → 已提前为第一优先级

**本阶段已独立成篇：`docs/CLREQ-HORIZONTAL-PLAN.md`（H0–H6，期望 100 人日）。**
横排不再是竖排完成后的可选补充，而是先行验证共享规则内核的场地：
H0 产出的 `punct-table.lua` / `adjust.lua` / `kinsoku.lua` 同时是竖排 P1/P2 的地基。

### P7 · 文档、声明与基线

- 完成 `docs/CLREQ-CONFORMANCE.md` 全量条款状态；README/Wiki 增加「clreq 符合度」章节。
- 三套回归基线整体复核与更新；`LEARNING.md` 记录踩坑。

**工作量：8–14 人日**

---

## 7. 工作量估算汇总

单位：人日，指**熟悉本代码库的开发者**，含实现 + unit test + 回归基线 + 文档。

| 阶段 | 乐观 | 期望 | 悲观 | 关键依赖 |
|------|------|------|------|----------|
| P0 基础设施与数据化 | 8 | 10 | 12 | — |
| P1 标点系统达标 | 18 | 22 | 28 | P0 |
| P2 弹性行内调整引擎 | 25 | 32 | 45 | P0, P1 |
| P3 中西文混排 | 14 | 18 | 24 | P2 |
| P4 行间注 · 注音 | 18 | 25 | 35 | P0（P2 可并行） |
| P5 页面与标题级规则 | 12 | 15 | 20 | P2 |
| P7 文档与基线 | 8 | 10 | 14 | 全部 |
| **小计（口径 A：直排完整）** | **103** | **132** | **178** | |
| P6 横排引擎（口径 C） | 40 | 55 | 80 | P2 |
| **合计（口径 C：全覆盖）** | **143** | **187** | **258** | |

参考换算：口径 A ≈ **5–9 人月**；口径 C ≈ **7–13 人月**。

规模侧写（用于交叉校验）：现有 `tex/` 约 3.0 万行（去除空行与注释后约 1.9 万行）。预计新增/改写
Lua 约 4000–6000 行、expl3 约 1200–2000 行、测试（unit + clreq 断言 + 用例 .tex）约 3000–5000 行。
测试与基线复核约占总工作量的 35%——这是竖排引擎的固有成本，不宜压缩。

**建议的最小可交付里程碑（M1）= P0 + P1 + P2 的禁则与悬挂部分**，约 **45–60 人日**，
即可对外声称「符合 clreq 标点与换行禁则（直排）」，这也是用户感知最强的一段。

---

## 8. 建议

0. **横排先行**：见 `docs/CLREQ-HORIZONTAL-PLAN.md`。新建的横排模式没有历史包袱，
   且能借 TeX 断行器省下大量求解器工作，是把共享规则内核跑通的最低成本路径；
   竖排随后独立改造，只接线不重写规则。
1. **先立规矩再写代码**：标点数据表与 clreq 断言测试是后续所有阶段的验收基座，
   不要跳过它直接改 `punct.lua`。
2. **按文档类分档启用**：新规则默认只在 `ltc-cn-vbook` / `ltc-tw-vbook` 生效，
   `ltc-guji` 通过显式开关选用。古籍复刻的既有版面不应因为「更符合现代规范」而改变。
3. **不追求 100%，追求可核查**：发布符合度矩阵，把「不实现」也写清楚理由
   （横排交给生态、bidi 非本引擎职责、儿化小字用字罕见等）。这比模糊地宣称「支持 clreq」更有价值。
4. **优先级排序建议**：禁则与悬挂（用户最易感知）> 标点宽度调整（版面质量）>
   中西混排（实际文档高频）> 注音（`ltc-tw-vbook` 差异化卖点）> 孤行孤字（长尾）。
5. **把 P2 单独立项评审**：它会触及 `layout-grid.lua`（2700 行）的核心路径，
   建议先出一份 `ai_must_read/` 下的设计文档，明确 cell 模型与求解器接口，再动手。

---

## 附：主要证据索引

| 主题 | 位置 |
|------|------|
| 标点分类表 | `tex/core/luatex-cn-core-punct.lua:137-206` |
| 直排字形替换表 | `tex/core/luatex-cn-core-punct.lua:212-242` |
| PUA / tounicode 还原 | `tex/core/luatex-cn-core-punct.lua:264-302` |
| 无条件挤压 0.5 格 | `tex/core/luatex-cn-core-punct.lua:634-641`、`tex/core/luatex-cn-layout-grid-helpers.lua:270-298` |
| 行尾悬挂未实现（TODO） | `tex/core/luatex-cn-core-punct.lua:669-671` |
| 大陆式偏移经验值 | `tex/core/luatex-cn-core-punct.lua:766-772` |
| grid 模式禁则钩子 | `tex/core/luatex-cn-core-punct.lua:399-486` |
| natural 模式挤进/推出代价比较 | `tex/core/luatex-cn-layout-grid.lua:1552-1626` |
| 字形旋转实现（可复用） | `tex/core/luatex-cn-core-render-page-process.lua:137-140` |
| 行间线（专名号/书名号） | `tex/decorate/luatex-cn-linemark.sty:98-108` |
| 着重号（默认 `○`，右侧） | `tex/decorate/luatex-cn-decorate.sty:138-143`、`tex/decorate/luatex-cn-decorate.lua:166-168` |
| 单行对齐（缺均排） | `tex/core/luatex-cn-core-content.sty:129-130`、`tex/core/luatex-cn-layout-grid.lua:542` |
| 插件生命周期接口 | `ai_must_read/design.md`「插件化架构与插件规范」 |
